# Cross-Lingual Chatbot Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3001

# Google Cloud Project IDs
# Replace with your actual Dialogflow project IDs
DIALOGFLOW_PROJECT_ID_SUPPORT=your-support-project-id
DIALOGFLOW_PROJECT_ID_SALES=your-sales-project-id

# Google Cloud Service Account Credentials
# Replace with paths to your actual credential files
GOOGLE_APPLICATION_CREDENTIALS_SUPPORT=./your-support-credentials.json
GOOGLE_APPLICATION_CREDENTIALS_SALES=./your-sales-credentials.json

# Optional: Additional Configuration
# NODE_ENV=development
# LOG_LEVEL=info

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace all placeholder values with your actual Google Cloud project details
# 3. Download your service account JSON files from Google Cloud Console
# 4. Place the JSON files in the backend directory
# 5. Update the paths above to match your JSON file names
# 6. Never commit the .env file or JSON credential files to version control